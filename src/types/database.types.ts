/**
 * PLACEHOLDER — replaced by generated output.
 *
 * Regenerate after every migration, and commit the result:
 *   npm run types:generate
 *
 * Until the first migration is pushed this file keeps the Supabase clients
 * compiling without pretending to know a schema that does not exist yet.
 */

export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[];

export type Database = Record<string, never>;
